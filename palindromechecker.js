function palindrome(str) {
    // Sanitize the string
    let isPalindrome = false;
    str = str.replace(/[^0-9A-Z]+/gi, "").toLowerCase();
    // Check if plaindrome
    return true;
}



palindrome("eye");
